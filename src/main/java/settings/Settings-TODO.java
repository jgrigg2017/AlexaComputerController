package main.java.settings;

import java.net.InetAddress;
import java.net.InetSocketAddress;

public class Settings {
	/*
	 * This url is the WAN ip address of the computer being controlled.
	 * You will likely need to set up port forwarding on your router in order to forward the
	 * HTTP requests to the appropriate computer running Server.java
	 */
	public static final String urlString = "";
    /*
     * This Id can be found on https://developer.amazon.com/edw/home.html#/ 
     * "Edit" the relevant Alexa Skill(s) and copy/paste the Application Id(s) in this Set.
     */
	public static final String applicationID = "";
}
